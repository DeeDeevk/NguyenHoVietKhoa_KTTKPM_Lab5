global:
  scrape_interval: 15s # Thu thập dữ liệu mỗi 15 giây

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node-exporter:9100'] # Kết nối tới service node-exporter